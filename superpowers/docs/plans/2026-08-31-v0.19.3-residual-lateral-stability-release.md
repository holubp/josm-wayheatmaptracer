# v0.19.3 residual lateral stability release

**Goal:** Publish the verified cleanup-enabled residual lateral stability correction as `v0.19.3`.

**Scope:** This patch keeps `LEGACY_V02`, cleanup defaults, detector ordering, and candidate ranking semantics unchanged. It improves only explicitly enabled corridor-aware geometry cleanup, diagnostics, and offline calibration tooling.

**Authorization:** The maintainer explicitly requested the `0.19.3` release on 2026-08-31. The historical private corpus does not contain an accepted format-10 cleaned candidate for replay; that limitation remains documented and is not represented as completed evidence.

## Release contents

- robust physical-distance trend/residual attribution for unsupported lateral ripple;
- one cleanup-only absolute short-wave turn cost inside the existing exact second-order DP;
- one endpoint-ownership gate across every cleanup-only optimizer contribution;
- boundary-censored direct-evidence windows that cannot authorize cleanup intervention;
- additive format-10 optimizer diagnostics and backward-compatible analyzers;
- bounded immutable nested-ZIP calibration inventory, privacy quarantine, and deterministic split locking;
- focused scale, endpoint, diagnostics, legacy-format, codec, and hostile-archive regressions.

## Publication contract

- Version and manifest: `0.19.3` / `Plugin-Version: 0.19.3`
- Tag and release title: `v0.19.3`
- Primary asset: `wayheatmaptracer.jar`
- Release type: non-draft, non-prerelease
- Recovery: preserve pushed state and report any failure; never force-push or rewrite a published tag.

## Verification

- clean Java test/build/Javadoc gate: 317 tests, zero failures/errors;
- Python tests and compilation: 29 tests plus 5 subtests;
- sampling-scale validation: 1,945 cases, maximum round-trip error `0.03146144458831657 m`;
- strict 14-archive redacted manifest scan: zero quarantined, manifest SHA-256 `7a69647240e06f06ca6d2567f9e28e2a132725955a713d097063c4953c2d20fb`;
- jar manifest: `Plugin-Version: 0.19.3`, `Implementation-Version: 0.19.3`;
- release jar SHA-256: `9d3c95e5244bb2a43163dc370d0ae985a59d443c5d93ebe6fd08f892a959a8a0`;
- local/remote commit and tag identity;
- GitHub release metadata and remote asset digest.
