# v0.19.1 corridor detector release

**Goal:** Publish the verified all-palette corridor-response implementation as `v0.19.1`.
**Why planning is required:** Commit, push, tag creation, and GitHub release publication change external repository state.
**Acceptance:** The release commit contains only the detector implementation, its tests and documentation, this release record, and the `0.19.1` version bump; unrelated local artifacts remain untracked; the full test and jar build pass; `main` and `v0.19.1` resolve to the release commit; GitHub exposes a non-draft, non-prerelease `v0.19.1` release with `wayheatmaptracer.jar`; the remote asset digest matches the locally built jar. Stop before publishing if review or verification fails. Recovery from a failed publication is to preserve the pushed commit/tag state and report the exact failure without force-pushing or rewriting history.

### Outcome 1: Patch release contents are verified
- Work: Review the corridor-response diff, bump `gradle.properties` to `0.19.1`, exclude unrelated artifacts, and build the exact release jar.
- Verify: `git diff --check && sh gradlew test jar`

### Outcome 2: Release commit and remote refs are published
- Work: Commit the reviewed files, push `main`, create and push `v0.19.1` at that commit, and preserve existing remote history.
- Verify: `git rev-parse HEAD && git ls-remote origin refs/heads/main refs/tags/v0.19.1`

### Outcome 3: GitHub release and asset are verified
- Work: Publish `v0.19.1` using the AudioWptMarker naming convention and upload the release jar as `wayheatmaptracer.jar`.
- Verify: `gh release view v0.19.1 --json tagName,name,isDraft,isPrerelease,url,targetCommitish,assets`
