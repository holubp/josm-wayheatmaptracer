# v0.20.1 Tile Reliability Execution

**Source plan:** `$HOME/downloads/josm-wayheatmaptracer-v0.20.0-tile-reliability-plan.md`
**Baseline:** `v0.20.0` / `46b2fa1d7a89b93505a520b53f2fc2177ed85a63`
**Worktree:** `/data/data/com.termux/files/usr/tmp/josm-slide-tile-reliability`
**Branch:** `codex/tile-reliability`
**Candidate classification:** `v0.20.1` patch; CP9 minor behavior is not authorized
**Status:** complete

## Scope Contract

Implement the plan's privacy-safe, bounded plugin-direct tile acquisition
foundation without changing successful raster inputs, detector/tracker behavior,
geometry, topology, or edit application. Preserve strict whole-slide failure for
an explicitly requested incomplete aggregate. Do not enable CP9 graceful
degradation, background alignment, or partial aggregate rendering.

## Checkpoint Ledger

| Checkpoint | Status | Evidence | Risks / rollback |
| --- | --- | --- | --- |
| CP0 baseline and red reproductions | complete | Baseline: 321 Java tests, 29 Python tests plus 5 subtests, 1,945 scale cases; focused coupling/generation tests failed red | rollback to `46b2fa1` |
| CP1 contracts and privacy-safe transport | complete | Structured contracts, strict URL builder, loopback Cookie/redirect/Retry-After tests; encapsulated `HttpURLConnection` selected because safe JOSM DEBUG suppression was not proven | remove new tile package |
| CP2 decoder, classifier, and positive cache | complete | v0.20 classifier constants retained; PNG/content/dimension/empty/placeholder/corrupt/oversize-cache tests green; atomic legacy-layout cache | retain old sampler path |
| CP3 bounded coordinator and failure suppression | complete | Single-flight 100:1, 1,000-call negative suppression, fake-clock TTL, auth/rate circuits, deadlines, retry, cancellation, stale generation, queue dispatch tests green | coordinator isolated behind consumer facades |
| CP4 fixed sampler integration | complete | Direct network/cache code removed; existing mosaic/projection/scale tests and full fixture suite green | compatibility cache-file helper retained |
| CP5 aggregate layer lifecycle/state machine | complete | Explicit complete/incomplete states, 5/5 rendering, bounded caches, asynchronous composition, retry-storm and destroy-race tests green | static visualization path retained |
| CP6 source-plan decoupling | complete | Pure matrix tests prove diagnostic visibility and alternative mappings request selected source only | source plan is independently reversible |
| CP7 generation consistency and source probe | complete | Four-field credential-generation, monotonic bypass, shared URL generation, asynchronous selected-only transparent-tile probe tests green | probe is status-only and does not alter alignment |
| CP8 diagnostics, UI, and documentation | complete | Format 11 `tile-acquisition.json`, source-plan metadata, truthful layer/probe status, README/DEVELOPMENT/AGENTS/PLANS updates | additive debug format; formats 1-10 remain readable |
| CP9 minor behavior gate | deferred | not authorized | strict aggregate failure retained |
| CP10 verification and review | complete | Final: 344 Java tests, 0 failures/errors, 1 skip; clean build; warning-free Javadoc; 29 Python tests plus 5 subtests; 1,945 scale cases; sentinel and diff scans clean; jar manifest 0.20.1/JOSM 19555 | no release/push authority inferred |

## Baseline

- JOSM main version: `19555`
- Gradle: `9.4.1`
- Runtime JDK: Termux OpenJDK `21.0.12`; project Java target remains 17
- Original checkout local artifacts are excluded from this worktree.

## Invariants

- No credential values, full signed URLs, response bodies, or credential-derived
  fingerprints in logs, exceptions, diagnostics, cache metadata, or UI.
- No network, disk reads, PNG decoding, blocking waits, or unbounded submission
  from Swing paint methods.
- Complete aggregate means all five palettes at every required zoom.
- Diagnostic aggregate visibility never changes alignment source acquisition.
- Existing selected-source and explicit aggregate failure semantics remain strict.
- Version is classified as patch-compatible `0.20.1`; no push/tag/release is authorized.

## Verification Evidence

- `sh gradlew clean test build javadoc`: green before final version classification; Javadoc was then reduced from 100 warnings to zero.
- `python3 -m pytest -q scripts/tests`: 29 passed, 5 subtests passed.
- `python3 scripts/validate-sampling-scale.py --pretty`: 1,945 cases, maximum round-trip error 0.03146144458831657 m.
- Deterministic local HTTP tests cover Cookie forwarding, redirect refusal, status taxonomy, bounded body/cache handling, and Retry-After parsing without public network access.
- CP9 remains deferred: no graceful partial detector aggregate, partial diagnostic raster, or background alignment was enabled.
- Independent review fixed inline aggregate rendering on EDT, late post-destroy publication races, oversized cache/PNG decode exposure, deadline-bounded timeouts, and arbitrary diagnostic reason strings before the final gate.
