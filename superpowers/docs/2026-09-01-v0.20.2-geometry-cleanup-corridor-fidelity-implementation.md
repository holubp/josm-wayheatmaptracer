## Implementation Log

**Completed:** 2026-09-01

### What changed
- Repaired cleanup activation and initial preview selection, added local fail-closed interval cleanup, and shared one 6/10/20 m wrinkle-versus-bend classifier across the exact optimizer, smoother, and simplifier.
- Preserved raw candidates and topology anchors, rejected nonadjacent or command-inconsistent anchor assignments, and added explicit raw/cleaned/partial preview outcomes.
- Added schema-2 preferences, format-12 coordinate-free diagnostics, backward-compatible analyzers, and bounded privacy-safe offline calibration tooling.

### Verification
- `sh gradlew --no-daemon clean test build javadoc --console=plain`: 369 tests passed, 1 skipped; build and Javadoc succeeded.
- `python3 -m pytest -q scripts/tests`: 50 tests and 5 subtests passed; Python compilation succeeded.
- Sampling-scale validation passed 1,945 cases; independent re-review found no Critical or High defect.

### Notes
- Private reference pairing remained ambiguous, so no private-example threshold tuning or holdout claim was made. Java and Python classifiers retain independent synthetic coverage; a shared parity fixture remains future calibration hardening.
- `0.20.2` is a patch release because it repairs documented cleanup, preview, and safety behavior that was intended to work in `0.20.x`; cleanup remains disabled by default and cleanup-disabled tracking is preserved.
